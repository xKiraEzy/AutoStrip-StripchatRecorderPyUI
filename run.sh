#!/bin/bash
source venv/bin/activate
python main.py
```

This assumes you're using a Unix-like system (e.g., macOS or Linux) with a virtual environment located in the `venv` directory. If your virtual environment has a different path, make sure to adjust the `source` command accordingly.